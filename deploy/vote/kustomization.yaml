apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vote-dev
  namespace: flux-system
spec:
  path: ./deploy/vote
  sourceRef:
    kind: GitRepository
    name: instavote
